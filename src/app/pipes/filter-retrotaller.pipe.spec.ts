import { FilterRetroTallerPipe } from './filter-retrotaller.pipe';

describe('FilterRetroTallerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRetroTallerPipe();
    expect(pipe).toBeTruthy();
  });
});
